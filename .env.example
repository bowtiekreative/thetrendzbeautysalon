# Copy to .env and fill in. Never commit the real .env.
PORT=3000

# MySQL — on Hostinger use 127.0.0.1 (NOT localhost).
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=trendz
DB_PASSWORD=change-me
DB_NAME=trendz
DB_POOL=10

# Admin credentials used to mint content-API keys (POST /api/keys).
ADMIN_USER=admin
ADMIN_PASSWORD=change-me
